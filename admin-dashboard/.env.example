# ============================================================
# KavachAI — Admin Dashboard Environment Variables
# Copy to .env and fill in your values.
# ============================================================

# Backend service URLs — replace with Railway public URLs for production
VITE_WORKER_URL=http://localhost:8001
VITE_POLICY_URL=http://localhost:8002
VITE_TRIGGER_URL=http://localhost:8003
VITE_CLAIMS_URL=http://localhost:8004
VITE_PAYMENT_URL=http://localhost:8005
VITE_ML_URL=http://localhost:8006

# Demo mode — set to "true" to use only mock data (no backend required)
VITE_DEMO_MODE=false
